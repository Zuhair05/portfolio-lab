<%- include('./partials/nav') %>

    <h1> Projects</h1>
    <% projects.forEach(project => { %>
        <div class="projects">
            
           
                <a href="/project/<%= project.id  %>">
                <h2><%= project.title %></h2>
                </a>
           
           
            </div>
        <% }); %>
</body>
</html>